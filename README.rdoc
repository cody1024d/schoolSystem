== README

This web app is for use in a school.

Currently, the application is seeded with a single student, and a single teacher.

Teacher email = a@gmail.com password = foobar
Student email = b@gmail.com password = foobar
